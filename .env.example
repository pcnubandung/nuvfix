# Environment Configuration
# Copy this file to .env and update the values

# Application Environment
NODE_ENV=production

# Server Configuration
PORT=3000

# Security
# IMPORTANT: Generate a strong random string for JWT_SECRET
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-min-32-characters-here

# Database
# For Railway: use /app/data/koperasi.db (with volume mounted)
# For local: use ./koperasi.db
DATABASE_PATH=./koperasi.db

# File Upload
# For Railway: use /app/data/uploads (with volume mounted)
# For local: use ./uploads
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=5242880

# Session
SESSION_TIMEOUT=24h

# CORS (Optional - for production)
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
